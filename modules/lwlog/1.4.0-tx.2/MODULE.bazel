"""Module definition for lwlog library."""
module(name = "lwlog", version = "1.4.0-tx.2")

bazel_dep(name = "rules_foreign_cc", version = "0.15.0")
bazel_dep(name = "platforms", version = "0.0.10")

# Maybe through @bazel_tools//tools/build_defs/repo:git.bzl it's possible to extend the content of the automatically loaded lwlog repository
# (which is declared in source.json)?
